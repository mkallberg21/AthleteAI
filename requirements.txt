# API
fastapi>=0.110
uvicorn>=0.29
# FastAPI needs this to read a form body. The sign-in endpoint accepts one so
# a phone whose JS module failed to load can still sign in -- which is exactly
# the case where a missing dependency would not be noticed until it mattered.
python-multipart>=0.0.9

# Storage is stdlib sqlite3 -- no database driver needed.

# Testing
pytest>=8.0
httpx>=0.27          # required by fastapi.testclient
